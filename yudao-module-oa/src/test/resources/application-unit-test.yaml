spring:
  main:
    lazy-initialization: true
    banner-mode: off
    allow-bean-definition-overriding: true

--- #################### 数据库相关配置 ####################

spring:
  datasource:
    name: ruoyi-vue-pro
    url: jdbc:h2:mem:testdb;MODE=MYSQL;DATABASE_TO_UPPER=false;NON_KEYWORDS=value,day,year,month;
    driver-class-name: org.h2.Driver
    username: sa
    password:
    druid:
      async-init: true
      initial-size: 1
  sql:
    init:
      schema-locations: classpath:/sql/create_tables.sql

  data:
    redis:
      host: 127.0.0.1
      port: 16379
      database: 0

mybatis:
  lazy-initialization: true

--- #################### 芋道相关配置 ####################

yudao:
  info:
    base-package: cn.iocoder.yudao.module
