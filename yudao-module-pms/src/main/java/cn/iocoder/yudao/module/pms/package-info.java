/**
 * pms 包下，项目管理（Project Management System）
 * 例如说：项目、工作事项、迭代、看板、工时和知识库等等
 *
 * 1. Controller URL：以 /pms/ 开头，避免和其它 Module 冲突
 * 2. DataObject 表名：以 pms_ 开头，方便在数据库中区分
 *
 * 注意，由于 PMS 模块下容易和其它模块重名，所以类名都加 Pms 前缀
 */
package cn.iocoder.yudao.module.pms;
