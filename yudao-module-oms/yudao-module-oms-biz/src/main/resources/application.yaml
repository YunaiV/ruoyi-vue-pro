spring:
  application:
    name: yudao-server-sale
  datasource:
    dynamic:
      druid:
        initial-size: 5
        min-idle: 10
        max-active: 20
        max-wait: 600000
        time-between-eviction-runs-millis: 60000
        min-evictable-idle-time-millis: 300000
        max-evictable-idle-time-millis: 900000
        validation-query: SELECT 1
        test-while-idle: true
        test-on-borrow: false
        test-on-return: false
      primary: master
      datasource:
        master:
          url: jdbc:mysql://192.168.10.151:3306/ruoyi-vue-pro?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&nullCatalogMeansCurrent=true # MySQL Connector/J 8.X 连接的示例
          username: it003
          password: asivJssflioKK
server:
  port: 8081

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

yudao:
  info:
    version: 1.0.0
    base-package: cn.iocoder.yudao

threePartyPlatform:
  shopify:
    refreshToken:
      enable: true
      cron: "0 0 0 1 1 ?"
  walmart:
    refreshToken:
      enable: true
      cron: "0 */10 * * * ?"