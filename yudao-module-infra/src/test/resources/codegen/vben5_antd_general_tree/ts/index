import type { PageParam, PageResult } from '@vben/request';
import type { Dayjs } from 'dayjs';

import { requestClient } from '#/api/request';

export namespace CategoryApi {
    /** 分类信息 */
  export interface Category {
            id: number; // 编号
            name?: string; // 名字
            parentId?: number; // 父编号
      children?: Category[];
      }
}

/** 查询分类列表 */
export function getCategoryList(params: any) {
  return requestClient.get<CategoryApi.Category[]>('/infra/category/list', { params });
}

/** 查询分类详情 */
export function getCategory(id: number) {
  return requestClient.get<CategoryApi.Category>(`/infra/category/get?id=${id}`);
}

/** 新增分类 */
export function createCategory(data: CategoryApi.Category) {
  return requestClient.post('/infra/category/create', data);
}

/** 修改分类 */
export function updateCategory(data: CategoryApi.Category) {
  return requestClient.put('/infra/category/update', data);
}

/** 删除分类 */
export function deleteCategory(id: number) {
  return requestClient.delete(`/infra/category/delete?id=${id}`);
}


/** 导出分类 */
export function exportCategory(params: any) {
  return requestClient.download('/infra/category/export-excel', { params });
}