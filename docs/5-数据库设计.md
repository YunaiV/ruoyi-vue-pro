# 5. 数据库设计

> 配套 [1-产品愿景.md](1-产品愿景.md) 使用。技术架构见 [4-技术架构.md](4-技术架构.md)。算法见 [6-核心算法.md](6-核心算法.md)。
> 数据库：MySQL 8.0，字符集 `utf8mb4_unicode_ci`
> 命名约定：表 `t_` 前缀（C 端业务表）/ `system_` 前缀（芋道系统表），蛇形命名；主键统一 `id BIGINT UNSIGNED AUTO_INCREMENT`

---

## 1. 实体关系总览

```
                ┌───────────────┐
                │   t_user      │ (含位置/信用分/实名)
                └───┬───────┬───┘
                    │       │
              creator│       │participant
                    │       │
        ┌───────────▼─────┐ │  ┌──────────────────┐
        │   t_activity    │◄┼──┤t_activity_member │
        │  (含公开字段)    │ │  └──────────────────┘
        └─┬───┬───┬───────┘ │
          │   │   │         │  ┌────────────────────────┐
          │   │   │         └──┤t_activity_application │ (新增)
          │   │   │            │ (陌生人申请加入)         │
          │   │   │            └────────────────────────┘
          │   │   │
          │   │   └─────────────► t_matchup (对阵表)
          │   │                          │
          │   │                          └─► t_match (单场比赛)
          │   │
          │   └─► t_event_post (战报)
          │
          └─► t_competition (比赛：接力赛/淘汰赛)
                    │
                    ├─► t_competition_team
                    └─► t_match (单场比赛 - 比赛模式)

         ┌──────────────────┐
         │ t_rating_test    │ (段位评测)
         └────────┬─────────┘
                  └─► t_rating_result

         ┌──────────────────┐
         │ t_user_rating    │ (用户战绩积分)
         └──────────────────┘

         ┌──────────────────┐         ┌──────────────────┐
         │ t_user_blacklist │ (新增)  │  t_user_report   │ (新增)
         │ (召集人黑名单)    │         │  (举报记录)        │
         └──────────────────┘         └──────────────────┘

         ┌──────────────────┐
         │   t_club         │ (球队)
         └────┬─────────────┘
              └─► t_club_member

         ┌──────────────────────────────────┐
         │  若依系统表（运营管理后台）        │
         │  sys_user / sys_role / sys_dict  │
         │  sys_menu / sys_job / ...        │
         │  与 t_user 完全独立               │
         └──────────────────────────────────┘
```

---

## 2. 核心表设计

### 2.1 用户表 `t_user`（v1.1 扩展位置/信用/实名字段）

```sql
CREATE TABLE `t_user` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `openid` VARCHAR(64) NOT NULL COMMENT '微信 openid',
  `unionid` VARCHAR(64) DEFAULT NULL COMMENT '微信 unionid',
  `nickname` VARCHAR(64) DEFAULT NULL COMMENT '昵称',
  `avatar_url` VARCHAR(512) DEFAULT NULL COMMENT '头像 URL',
  `gender` TINYINT DEFAULT 0 COMMENT '0未知 1男 2女',
  `phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号（实名时获取）',
  `is_real_name_verified` TINYINT DEFAULT 0 COMMENT 'v1.1 是否实名（绑定phone）',
  `current_level` VARCHAR(8) DEFAULT NULL COMMENT '当前段位 D1/D2/.../S',
  `current_score` INT DEFAULT 0 COMMENT '当前评分（0-100）',
  `total_games` INT DEFAULT 0 COMMENT '总比赛场次',
  `wins` INT DEFAULT 0 COMMENT '总胜场',
  `total_organized` INT DEFAULT 0 COMMENT 'v1.1 已组织活动数（召集人成长等级）',
  `credit_score` INT DEFAULT 100 COMMENT 'v1.1 信用分（默认100，范围0-150）',
  `last_known_lng` DECIMAL(10,6) DEFAULT NULL COMMENT 'v1.1 最近位置经度（脱敏到街道）',
  `last_known_lat` DECIMAL(10,6) DEFAULT NULL COMMENT 'v1.1 最近位置纬度（脱敏到街道）',
  `region_code` VARCHAR(16) DEFAULT NULL COMMENT 'v1.1 行政区划代码（如310104）',
  `region_name` VARCHAR(64) DEFAULT NULL COMMENT 'v1.1 行政区名（如上海·徐汇区）',
  `last_location_at` DATETIME DEFAULT NULL COMMENT 'v1.1 最近位置更新时间',
  `female_visibility` TINYINT DEFAULT 0 COMMENT 'v1.1 女性可见设置 0仅活动内 1完全公开',
  `discover_enabled` TINYINT DEFAULT 1 COMMENT 'v1.1 是否参与附近发现（个人可关闭）',
  `status` TINYINT DEFAULT 1 COMMENT '1正常 0封禁',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_openid` (`openid`),
  KEY `idx_unionid` (`unionid`),
  KEY `idx_region` (`region_code`),
  KEY `idx_credit` (`credit_score`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='C端用户表（与若依sys_user分离）';
```

### 2.2 球队表 `t_club`

```sql
CREATE TABLE `t_club` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(64) NOT NULL COMMENT '球队名',
  `logo_url` VARCHAR(512) DEFAULT NULL,
  `description` VARCHAR(255) DEFAULT NULL,
  `city` VARCHAR(32) DEFAULT NULL,
  `owner_id` BIGINT UNSIGNED NOT NULL COMMENT '队长用户ID',
  `member_count` INT DEFAULT 1,
  `total_activities` INT DEFAULT 0,
  `status` TINYINT DEFAULT 1,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_owner` (`owner_id`),
  KEY `idx_city` (`city`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='球队';
```

### 2.3 球队成员 `t_club_member`

```sql
CREATE TABLE `t_club_member` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `club_id` BIGINT UNSIGNED NOT NULL,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `role` TINYINT NOT NULL DEFAULT 0 COMMENT '0成员 1管理员 2队长',
  `nickname_in_club` VARCHAR(64) DEFAULT NULL COMMENT '队内昵称',
  `joined_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_club_user` (`club_id`, `user_id`),
  KEY `idx_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='球队成员';
```

---

## 3. 活动相关表

### 3.1 活动表 `t_activity`（v1.1 扩展公开字段）

```sql
CREATE TABLE `t_activity` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `short_code` VARCHAR(8) NOT NULL COMMENT '活动短码（6位）',
  `title` VARCHAR(128) NOT NULL,
  `creator_id` BIGINT UNSIGNED NOT NULL,
  `club_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '可选关联球队',
  `venue_name` VARCHAR(128) NOT NULL COMMENT '场馆名',
  `venue_address` VARCHAR(255) DEFAULT NULL,
  `venue_lat` DECIMAL(10,6) DEFAULT NULL,
  `venue_lng` DECIMAL(10,6) DEFAULT NULL,
  `start_time` DATETIME NOT NULL,
  `end_time` DATETIME NOT NULL,
  `court_count` INT NOT NULL DEFAULT 1 COMMENT '场地数',
  `planned_count` INT NOT NULL DEFAULT 6 COMMENT '计划人数',
  `current_count` INT NOT NULL DEFAULT 0 COMMENT '当前已报名',
  `mode` VARCHAR(16) NOT NULL DEFAULT 'MIXED' COMMENT 'MEN_DOUBLES/WOMEN_DOUBLES/MIXED/FREE',
  `rotation` VARCHAR(16) NOT NULL DEFAULT 'ROTATE' COMMENT 'FIXED/ROTATE',
  `match_duration` INT NOT NULL DEFAULT 15 COMMENT '单场分钟',
  `aa_amount` DECIMAL(8,2) DEFAULT NULL COMMENT 'AA 金额',
  `remark` VARCHAR(500) DEFAULT NULL,
  `is_public` TINYINT NOT NULL DEFAULT 0 COMMENT 'v1.1 是否对外公开',
  `visible_radius_km` INT NOT NULL DEFAULT 3 COMMENT 'v1.1 可见半径（1/3/5/10）',
  `accept_strangers` TINYINT NOT NULL DEFAULT 1 COMMENT 'v1.1 是否接受陌生人申请',
  `need_review` TINYINT NOT NULL DEFAULT 1 COMMENT 'v1.1 申请是否需审核',
  `limit_levels` VARCHAR(128) DEFAULT NULL COMMENT 'v1.1 段位限制 JSON 数组，如["C1","C2","B1"]',
  `limit_gender` TINYINT DEFAULT 0 COMMENT 'v1.1 性别限制 0不限 1只缺男 2只缺女',
  `limit_strangers_count` INT DEFAULT 1 COMMENT 'v1.1 通过陌生申请最多多少人',
  `accepted_strangers_count` INT DEFAULT 0 COMMENT 'v1.1 已通过陌生申请人数',
  `status` VARCHAR(16) NOT NULL DEFAULT 'RECRUITING' COMMENT 'RECRUITING/MATCHED/PLAYING/FINISHED/CANCELLED',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_short_code` (`short_code`),
  KEY `idx_creator_status` (`creator_id`, `status`),
  KEY `idx_start_time` (`start_time`),
  KEY `idx_club` (`club_id`),
  KEY `idx_public_status` (`is_public`, `status`, `start_time`) COMMENT 'v1.1 发现页查询',
  SPATIAL KEY `spatial_venue` (`venue_lng`, `venue_lat`) COMMENT 'v1.1 地理位置查询（MySQL 8 空间索引）'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='活动';
```

> **MySQL 8 空间索引提示**：v1 用 `(venue_lng - userLng)² + (venue_lat - userLat)²` 做简单距离计算即可；用户量上 5w 后切 PostGIS 或 Elasticsearch geo_distance。

### 3.2 活动参与者 `t_activity_member`

```sql
CREATE TABLE `t_activity_member` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `activity_id` BIGINT UNSIGNED NOT NULL,
  `user_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '注册用户ID',
  `placeholder_name` VARCHAR(32) DEFAULT NULL COMMENT '临时占位人名（未注册）',
  `gender` TINYINT NOT NULL DEFAULT 0 COMMENT '0未知 1男 2女',
  `level_at_join` VARCHAR(8) DEFAULT NULL COMMENT '报名时的段位（用于排对阵）',
  `join_source` VARCHAR(16) NOT NULL DEFAULT 'INVITE' COMMENT 'v1.1 来源 INVITE/MANUAL/DISCOVERY',
  `joined_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `status` VARCHAR(16) NOT NULL DEFAULT 'JOINED' COMMENT 'JOINED/WAITING/QUIT/REMOVED/NO_SHOW',
  `note` VARCHAR(255) DEFAULT NULL COMMENT '备注，如 30 分钟后到',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_activity_user` (`activity_id`, `user_id`),
  KEY `idx_activity` (`activity_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='活动参与者';
```

### 3.3 活动申请记录 `t_activity_application`（v1.1 新增）

```sql
CREATE TABLE `t_activity_application` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `activity_id` BIGINT UNSIGNED NOT NULL,
  `applicant_id` BIGINT UNSIGNED NOT NULL COMMENT '申请人用户ID',
  `applicant_snapshot` JSON NOT NULL COMMENT '申请时的快照（性别/段位/信用分/历史活动数）',
  `reason` VARCHAR(255) DEFAULT NULL COMMENT '申请理由',
  `distance_km` DECIMAL(6,2) DEFAULT NULL COMMENT '申请时距离场馆 km',
  `status` VARCHAR(16) NOT NULL DEFAULT 'PENDING' COMMENT 'PENDING/APPROVED/REJECTED/EXPIRED/CANCELLED',
  `reviewer_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '审核人（一般是召集人）',
  `review_reason` VARCHAR(255) DEFAULT NULL COMMENT '审核理由（拒绝时填）',
  `reviewed_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_activity_applicant` (`activity_id`, `applicant_id`),
  KEY `idx_activity_status` (`activity_id`, `status`),
  KEY `idx_applicant` (`applicant_id`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='活动申请加入记录';
```

### 3.4 对阵表 `t_matchup`

```sql
CREATE TABLE `t_matchup` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `activity_id` BIGINT UNSIGNED NOT NULL,
  `version` INT NOT NULL DEFAULT 1 COMMENT '对阵表版本（重新生成会+1）',
  `total_rounds` INT NOT NULL COMMENT '总轮数',
  `rounds_data` JSON NOT NULL COMMENT '对阵表完整 JSON',
  `is_current` TINYINT NOT NULL DEFAULT 1 COMMENT '是否当前版本',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_activity_current` (`activity_id`, `is_current`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='对阵表';
```

**`rounds_data` JSON 结构**：见 [1-产品愿景.md §模块一 活动接龙+智能对阵表](1-产品愿景.md#模块一活动接龙--智能对阵表)。

### 3.5 单场比赛记录 `t_match`

```sql
CREATE TABLE `t_match` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `activity_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '关联活动（活动模式）',
  `competition_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '关联比赛（比赛模式）',
  `round_no` INT DEFAULT NULL COMMENT '所属轮次（活动模式）',
  `court_no` INT DEFAULT NULL COMMENT '场地号',
  `match_type` VARCHAR(16) NOT NULL DEFAULT 'DOUBLES' COMMENT 'SINGLES/DOUBLES/MIXED_DOUBLES',
  `team1_members` JSON NOT NULL COMMENT '队伍1成员 [{userId, name}]',
  `team2_members` JSON NOT NULL COMMENT '队伍2成员',
  `score1` INT NOT NULL DEFAULT 0,
  `score2` INT NOT NULL DEFAULT 0,
  `winner` TINYINT DEFAULT NULL COMMENT '1队伍1胜 2队伍2胜',
  `status` VARCHAR(16) NOT NULL DEFAULT 'PENDING' COMMENT 'PENDING/PLAYING/FINISHED',
  `started_at` DATETIME DEFAULT NULL,
  `finished_at` DATETIME DEFAULT NULL,
  `referee_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '裁判用户ID',
  PRIMARY KEY (`id`),
  KEY `idx_activity` (`activity_id`),
  KEY `idx_competition` (`competition_id`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='单场比赛';
```

---

## 4. 比赛模块表

### 4.1 比赛主表 `t_competition`

```sql
CREATE TABLE `t_competition` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `short_code` VARCHAR(8) NOT NULL,
  `name` VARCHAR(128) NOT NULL,
  `type` VARCHAR(16) NOT NULL COMMENT 'RELAY/KNOCKOUT',
  `creator_id` BIGINT UNSIGNED NOT NULL,
  `config` JSON NOT NULL COMMENT '比赛配置',
  `status` VARCHAR(16) NOT NULL DEFAULT 'CREATED' COMMENT 'CREATED/PLAYING/FINISHED',
  `started_at` DATETIME DEFAULT NULL,
  `finished_at` DATETIME DEFAULT NULL,
  `winner_team_id` BIGINT UNSIGNED DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_short_code` (`short_code`),
  KEY `idx_creator` (`creator_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='比赛';
```

### 4.2 比赛队伍 `t_competition_team`

```sql
CREATE TABLE `t_competition_team` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `competition_id` BIGINT UNSIGNED NOT NULL,
  `team_name` VARCHAR(64) NOT NULL,
  `members` JSON NOT NULL COMMENT '队员列表',
  `total_score` INT DEFAULT 0 COMMENT '接力赛累计分',
  `wins` INT DEFAULT 0,
  `losses` INT DEFAULT 0,
  PRIMARY KEY (`id`),
  KEY `idx_competition` (`competition_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='比赛队伍';
```

---

## 5. 段位评测表

### 5.1 评测题库 `t_rating_question`

```sql
CREATE TABLE `t_rating_question` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `category` VARCHAR(16) NOT NULL COMMENT 'BASIC/TECH/TACTIC/EXP',
  `question_text` VARCHAR(500) NOT NULL,
  `question_type` VARCHAR(16) NOT NULL COMMENT 'SINGLE/MULTI',
  `options` JSON NOT NULL COMMENT '选项列表 [{key, text, score, weight}]',
  `weight` DECIMAL(4,2) NOT NULL DEFAULT 1.0 COMMENT '题目权重',
  `enabled` TINYINT NOT NULL DEFAULT 1,
  `display_order` INT NOT NULL DEFAULT 0,
  PRIMARY KEY (`id`),
  KEY `idx_category_enabled` (`category`, `enabled`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评测题';
```

### 5.2 评测记录 `t_rating_test`

```sql
CREATE TABLE `t_rating_test` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `answers` JSON NOT NULL COMMENT '答题记录',
  `total_score` INT NOT NULL,
  `level_code` VARCHAR(8) NOT NULL COMMENT 'D1/D2/.../S',
  `tech_score` INT NOT NULL,
  `tactic_score` INT NOT NULL,
  `radar_data` JSON DEFAULT NULL COMMENT '雷达图6维数据',
  `tested_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `is_shared` TINYINT NOT NULL DEFAULT 0,
  PRIMARY KEY (`id`),
  KEY `idx_user_tested` (`user_id`, `tested_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评测记录';
```

---

### 5.3 AI 教练对话表 `t_ai_coach_log`（v1.0 新增）

```sql
CREATE TABLE `t_ai_coach_log` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `question_type` VARCHAR(16) NOT NULL COMMENT 'GRIP/STANCE/EQUIPMENT/FREE',
  `user_text` VARCHAR(500) DEFAULT NULL COMMENT '用户补充文字（可选）',
  `image_url` VARCHAR(512) DEFAULT NULL COMMENT 'COS 图片 URL（7 天后清理）',
  `ai_provider` VARCHAR(16) NOT NULL COMMENT 'CLAUDE/OPENAI/DOUBAO',
  `ai_model` VARCHAR(32) NOT NULL COMMENT '具体模型版本',
  `ai_response` TEXT NOT NULL COMMENT 'AI 回答内容',
  `tokens_used` INT DEFAULT 0 COMMENT 'token 消耗',
  `cost_cents` INT DEFAULT 0 COMMENT '本次调用成本（分）',
  `user_feedback` TINYINT DEFAULT NULL COMMENT '1有用 0没用 NULL未反馈',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_time` (`user_id`, `created_at`),
  KEY `idx_feedback` (`user_feedback`, `created_at`) COMMENT 'v2 训练数据筛选'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='AI 教练对话记录';
```

> 详见 [1-产品愿景.md §模块四 段位评测+AI 教练彩蛋](1-产品愿景.md#模块四段位评测--ai-教练彩蛋) 和 [§AI 能力路线](1-产品愿景.md#ai-能力路线)。

### 5.4 训练成长相关表（v1.2 新增）

#### 5.4.1 技能定义表 `t_skill`

```sql
CREATE TABLE `t_skill` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `code` VARCHAR(32) NOT NULL COMMENT '技能码 SMASH_DIAGONAL/BACKHAND_LIFT/...',
  `name` VARCHAR(64) NOT NULL COMMENT '技能名 如"斜线杀球"',
  `category` VARCHAR(16) NOT NULL COMMENT 'BASIC/OFFENSE/DEFENSE/TACTIC',
  `parent_code` VARCHAR(32) DEFAULT NULL COMMENT '父技能码（杀球→斜线杀球）',
  `difficulty` TINYINT NOT NULL COMMENT '1初/2中/3高',
  `min_level` VARCHAR(8) NOT NULL COMMENT '最低段位要求 D1-S',
  `description` VARCHAR(500) DEFAULT NULL,
  `self_check_questions` JSON DEFAULT NULL COMMENT '自查清单 [{q, options}]',
  `display_order` INT DEFAULT 0,
  `enabled` TINYINT DEFAULT 1,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_code` (`code`),
  KEY `idx_category` (`category`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='技能定义表（技能树节点）';
```

#### 5.4.2 外链教学视频表 `t_external_video`

```sql
CREATE TABLE `t_external_video` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `platform` VARCHAR(16) NOT NULL COMMENT 'BILIBILI/VIDEO_ACCOUNT/DOUYIN/YOUTUBE/SELF',
  `url` VARCHAR(512) NOT NULL,
  `title` VARCHAR(255) NOT NULL,
  `duration_sec` INT DEFAULT NULL,
  `cover_url` VARCHAR(512) DEFAULT NULL,
  `recommend_reason` VARCHAR(255) DEFAULT NULL COMMENT '为什么推荐这个视频',
  `quality_score` TINYINT DEFAULT 3 COMMENT '1-5 内容质量评分（运营打）',
  `is_available` TINYINT DEFAULT 1 COMMENT '0已失效 1可用',
  `last_check_at` DATETIME DEFAULT NULL,
  `view_count` INT DEFAULT 0 COMMENT '小程序内打开次数',
  `useful_count` INT DEFAULT 0 COMMENT '"觉得有用"反馈数',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_available` (`is_available`),
  KEY `idx_platform` (`platform`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='外链教学视频';
```

#### 5.4.3 技能-视频关联表 `t_skill_video`

```sql
CREATE TABLE `t_skill_video` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `skill_id` BIGINT UNSIGNED NOT NULL,
  `video_id` BIGINT UNSIGNED NOT NULL,
  `target_level` VARCHAR(8) DEFAULT NULL COMMENT '推荐给哪个段位',
  `priority` INT DEFAULT 0 COMMENT '排序优先级',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_skill_video` (`skill_id`, `video_id`),
  KEY `idx_skill` (`skill_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='技能-视频关联';
```

#### 5.4.4 训练菜单表 `t_training_menu`

```sql
CREATE TABLE `t_training_menu` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `skill_id` BIGINT UNSIGNED NOT NULL,
  `name` VARCHAR(128) NOT NULL COMMENT '菜单名 如"杀球落点训练"',
  `venue_type` VARCHAR(16) NOT NULL COMMENT 'HOME/COURT_NO_PARTNER/COURT_WITH_PARTNER',
  `duration_min` INT NOT NULL COMMENT '预计耗时（分钟）',
  `steps` JSON NOT NULL COMMENT '训练步骤 [{name, sets, reps, rest_sec, tips}]',
  `target_level` VARCHAR(8) DEFAULT NULL,
  `display_order` INT DEFAULT 0,
  `enabled` TINYINT DEFAULT 1,
  PRIMARY KEY (`id`),
  KEY `idx_skill_venue` (`skill_id`, `venue_type`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='训练菜单';
```

#### 5.4.5 用户训练计划表 `t_user_training_plan`

```sql
CREATE TABLE `t_user_training_plan` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `start_level` VARCHAR(8) NOT NULL,
  `target_level` VARCHAR(8) NOT NULL,
  `weak_skills` JSON NOT NULL COMMENT '本计划针对的弱项技能 codes',
  `total_weeks` INT NOT NULL DEFAULT 4,
  `weekly_frequency` INT NOT NULL DEFAULT 2 COMMENT '每周训练次数',
  `weekly_duration_min` INT NOT NULL DEFAULT 60 COMMENT '每次时长',
  `venue_type` VARCHAR(16) NOT NULL DEFAULT 'COURT_WITH_PARTNER',
  `plan_data` JSON NOT NULL COMMENT '完整计划详情 {weeks: [{weekNo, tasks: [...]}]}',
  `start_date` DATE NOT NULL,
  `end_date` DATE NOT NULL,
  `status` VARCHAR(16) NOT NULL DEFAULT 'ACTIVE' COMMENT 'ACTIVE/COMPLETED/ABANDONED',
  `completed_at` DATETIME DEFAULT NULL,
  `retest_test_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '完成后复测的 t_rating_test.id',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_status` (`user_id`, `status`),
  KEY `idx_user_date` (`user_id`, `start_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户训练计划';
```

**`plan_data` JSON 结构示例**：
```json
{
  "weeks": [
    {
      "weekNo": 1,
      "theme": "基础打牢",
      "tasks": [
        {
          "dayNo": 1,
          "skillCode": "BACKHAND_LIFT",
          "menuId": 23,
          "videoIds": [101, 102]
        }
      ]
    }
  ]
}
```

#### 5.4.6 训练打卡表 `t_user_training_checkin`

```sql
CREATE TABLE `t_user_training_checkin` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `plan_id` BIGINT UNSIGNED NOT NULL,
  `week_no` INT NOT NULL,
  `day_no` INT NOT NULL,
  `skill_id` BIGINT UNSIGNED NOT NULL,
  `menu_id` BIGINT UNSIGNED DEFAULT NULL,
  `completed_steps` JSON DEFAULT NULL COMMENT '完成了菜单中的哪些步骤',
  `actual_duration_min` INT DEFAULT NULL,
  `self_check_score` INT DEFAULT NULL COMMENT '自查清单得分',
  `note` VARCHAR(500) DEFAULT NULL COMMENT '练习心得',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_plan` (`user_id`, `plan_id`),
  KEY `idx_user_date` (`user_id`, `created_at`),
  KEY `idx_skill` (`skill_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='训练打卡记录';
```

#### 5.4.7 用户技能掌握度表 `t_user_skill`

```sql
CREATE TABLE `t_user_skill` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `skill_id` BIGINT UNSIGNED NOT NULL,
  `mastery_score` INT NOT NULL DEFAULT 0 COMMENT '掌握度 0-100',
  `practice_count` INT NOT NULL DEFAULT 0 COMMENT '累计练习次数',
  `last_practice_at` DATETIME DEFAULT NULL,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_skill` (`user_id`, `skill_id`),
  KEY `idx_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户技能掌握度（按打卡 + 复测变化更新）';
```

---

## 6. 信用与安全表（v1.1 新增）

### 6.1 用户信用变更日志 `t_user_credit_log`

```sql
CREATE TABLE `t_user_credit_log` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `score_before` INT NOT NULL,
  `score_after` INT NOT NULL,
  `score_delta` INT NOT NULL,
  `reason` VARCHAR(32) NOT NULL COMMENT 'NO_SHOW/ON_TIME/REPORTED/MANUAL_ADJUST/...',
  `ref_type` VARCHAR(16) DEFAULT NULL COMMENT '关联对象类型 ACTIVITY/REPORT',
  `ref_id` BIGINT UNSIGNED DEFAULT NULL,
  `remark` VARCHAR(255) DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_time` (`user_id`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='信用变更日志';
```

### 6.2 召集人黑名单 `t_user_blacklist`

```sql
CREATE TABLE `t_user_blacklist` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `owner_id` BIGINT UNSIGNED NOT NULL COMMENT '召集人ID',
  `blocked_user_id` BIGINT UNSIGNED NOT NULL COMMENT '被拉黑用户ID',
  `reason` VARCHAR(255) DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_owner_blocked` (`owner_id`, `blocked_user_id`),
  KEY `idx_blocked` (`blocked_user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='召集人黑名单';
```

### 6.3 举报记录 `t_user_report`

```sql
CREATE TABLE `t_user_report` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `reporter_id` BIGINT UNSIGNED NOT NULL,
  `reported_user_id` BIGINT UNSIGNED NOT NULL,
  `activity_id` BIGINT UNSIGNED DEFAULT NULL,
  `category` VARCHAR(32) NOT NULL COMMENT 'HARASSMENT/NO_SHOW/FAKE_INFO/SPAM/OTHER',
  `description` VARCHAR(500) DEFAULT NULL,
  `evidence_urls` JSON DEFAULT NULL COMMENT '截图/聊天记录',
  `status` VARCHAR(16) NOT NULL DEFAULT 'PENDING' COMMENT 'PENDING/HANDLED/REJECTED',
  `handler_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '若依运营人员ID',
  `handle_result` VARCHAR(255) DEFAULT NULL,
  `handled_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_reported_status` (`reported_user_id`, `status`),
  KEY `idx_status_time` (`status`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='举报记录（在若依运营后台处理）';
```

---

## 7. 积分系统表（v2 加，先建表）

### 7.1 用户战绩积分变更日志 `t_user_rating_log`

```sql
CREATE TABLE `t_user_rating_log` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `match_id` BIGINT UNSIGNED DEFAULT NULL,
  `score_before` INT NOT NULL,
  `score_after` INT NOT NULL,
  `score_delta` INT NOT NULL COMMENT '可正可负',
  `reason` VARCHAR(32) NOT NULL COMMENT 'MATCH_WIN/MATCH_LOSE/EVALUATION/...',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_time` (`user_id`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='积分变更日志';
```

---

## 8. 战报海报表

### 8.1 海报记录 `t_event_post`

```sql
CREATE TABLE `t_event_post` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `source_type` VARCHAR(16) NOT NULL COMMENT 'ACTIVITY/COMPETITION/RATING',
  `source_id` BIGINT UNSIGNED NOT NULL,
  `template_id` INT NOT NULL COMMENT '海报模板 ID',
  `data` JSON NOT NULL COMMENT '海报渲染所需数据',
  `image_url` VARCHAR(512) DEFAULT NULL COMMENT '已生成的海报图 URL',
  `share_count` INT DEFAULT 0,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_source` (`source_type`, `source_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='战报海报';
```

---

## 9. 芋道系统表（保留默认即可）

直接使用 YunaiV/ruoyi-vue-pro（芋道）自带的：

| 表 | 用途 |
|---|---|
| `system_users` | 运营账号（与 t_user 完全分离）|
| `system_role` | 角色（管理员、内容审核、客服）|
| `system_menu` | 菜单（小程序内容不显示这些菜单）|
| `system_dict_type` / `system_dict_data` | 字典（段位列表、模式枚举、敏感词）|
| `system_oauth2_client` | OAuth2 客户端（区分 admin / app 两端登录）|
| `system_social_user` | 微信小程序 openid / unionid 关联 |
| `infra_job` | 定时任务（每日统计、活动到期清理、信用分自动恢复）|
| `system_operate_log` | 操作日志 |
| `system_login_log` | 登录日志 |

**关键约束**：
- 不在 `system_users` 中放 C 端用户，只放公司内部账号
- 不在 `t_user` 中接入芋道运营权限模型；C 端通过 `/app-api/*` 走独立 SecurityConfig
- 芋道默认表前缀是 `system_` / `infra_`，不是若依老版本的 `sys_`

---

## 10. Redis 数据结构

### 10.1 活动接龙实时状态
- Key: `activity:{id}:members`
- Type: Hash
- TTL: 活动结束 + 1 天

### 10.2 实时比分推送
- Key: `match:{id}:live`
- Type: Pub/Sub Channel

### 10.3 排行榜
- Key: `leaderboard:weekly:{cityCode}`
- Type: Sorted Set
- TTL: 7 天

### 10.4 附近活动缓存（v1.1 新增）
- Key: `discovery:nearby:{regionCode}`
- Type: Sorted Set（Score 为综合排序权重）
- TTL: 5 分钟（活动状态变化时主动失效）

### 10.5 用户黑名单缓存（v1.1 新增）
- Key: `blacklist:owner:{ownerId}`
- Type: Set（被拉黑用户ID集合）
- TTL: 1 天

### 10.6 接口限流
- Key: `ratelimit:{api}:{userId}`
- Type: String + INCR + EXPIRE

---

## 11. 索引设计要点

| 表 | 关键索引 | 用途 |
|---|---|---|
| `t_user` | `uk_openid` | 登录查询 |
| `t_user` | `idx_region` | 同城用户筛选 |
| `t_user` | `idx_credit` | 信用分排行 |
| `t_activity` | `idx_creator_status` | 我的活动列表 |
| `t_activity` | `idx_start_time` | 按时间查活动 |
| `t_activity` | `idx_public_status` | 发现页核心查询 |
| `t_activity` | `spatial_venue` | 地理位置查询 |
| `t_activity_member` | `uk_activity_user` | 防重报名 |
| `t_activity_application` | `idx_activity_status` | 召集人审核列表 |
| `t_activity_application` | `idx_applicant` | 我的申请历史 |
| `t_match` | `idx_activity` | 活动战报聚合 |
| `t_rating_test` | `idx_user_tested` | 用户评测历史 |
| `t_user_credit_log` | `idx_user_time` | 信用变化曲线 |

---

## 12. 数据量预估（6 个月后 MAU 1 万）

| 表 | 日增 | 6 月累计 |
|---|---|---|
| `t_user` | 100 | ~30000 |
| `t_activity` | 200 | ~36000 |
| `t_activity_member` | 1500 | ~270000 |
| `t_activity_application` | 200 | ~36000 |
| `t_match` | 1200 | ~216000 |
| `t_rating_test` | 50 | ~9000 |
| `t_user_credit_log` | 500 | ~90000 |
| `t_user_report` | 5 | ~900 |
| `t_ai_coach_log` | 500 | ~90000 |
| `t_user_training_plan` | 100 | ~18000 |
| `t_user_training_checkin` | 400 | ~72000 |
| `t_user_skill` | 200 | ~36000 |

→ 单表数据量都不大，**MySQL 单实例够撑**，不需要分库分表。
